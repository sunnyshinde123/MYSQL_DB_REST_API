<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>ID : <%=data.id%></h3>
    <h5>Username : <%=data.username%></h5>
    <form method="post" action="/users/<%=data.id%>?_method=DELETE">
        <br><br>
        <label for="email">Email :</label>
        <input type="email" id="email" name="email" placeholder="email">
        <br><br>
        <label for="password">Password :</label>
        <input type="password" id="password" name="password" placeholder="password" onchange="checkPassword('<%=data.password%>')" required>
        <br><br>
        <button>Delete</button>
    </form>
    <script>
        function checkPassword(data){
            let password=document.getElementById("password").value;
            if(password!=data){
                alert("Wrong Password entered! pls try again...");
                document.getElementById("password").value="";
            }
        }
    </script>
</body>
</html>